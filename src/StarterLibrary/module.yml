name: WebsiteModules
description: A collection of components for building websites.
page_templates:
  - name: About
    components:
      - name: Block
        preset: centered_tight
      - name: List
        preset: medium
version: preset
